services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: jobsrss
      POSTGRES_PASSWORD: jobsrss
      POSTGRES_DB: jobsrss
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  api:
    build:
      context: ./backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    restart: unless-stopped

  worker:
    build:
      context: ./backend
    command: python worker.py
    env_file:
      - .env
    depends_on:
      - postgres
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    command: npm run start -- --hostname 0.0.0.0 --port 3000
    ports:
      - "3000:3000"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  postgres_data:
